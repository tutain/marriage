server:
  port: 80


spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_ADDRESS}/marriage
    username: ${MYSQL_USERNAME}
    password: ${MySQL_PASSWORD}


appid: ${APP_ID}
secret: ${APP_SECRET}

mybatis:
  mapper-locations: classpath*:mapper/*Mapper.xml
